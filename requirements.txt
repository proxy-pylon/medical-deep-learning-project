# Core scientific stack
numpy
pandas
scikit-learn

# PyTorch (CPU by default via pip; install a CUDA build separately if needed)
torch
torchvision

# TensorFlow and Keras
tensorflow
keras
keras-tuner


# Imaging & plots
pillow
matplotlib
seaborn
timm

# Optional: faster wheels and common deps get pulled automatically by the above
# typing-extensions
# tqdm
# scipy
# joblib

# Windows setup:
# in cmd: python -m venv .venv
# Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
# ".\.venv\Scripts\Activate.ps1"
# pip install -r requirements.txt

# Checking installed packages
# pip list

# Checking GPU availability
# python -c "import torch; print(torch.cuda.is_available())"

# Checking nvidia-smi in CMD
# nvidia-smi